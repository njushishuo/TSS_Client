<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="software.nju.tssclient">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.activity.common.LoginActivity"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activity.teacher.TeacherMainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".view.activity.student.StudentMainActivity"
            android:label="@string/app_name" />
        <activity android:name=".view.activity.teacher.StudentListActivity" />

        <activity
            android:name=".view.activity.common.AssignmentActivity"
            android:label="@string/title_activity_assignment"></activity>

        <activity
            android:name=".view.activity.common.QuestionListActivity"
            android:label="@string/title_activity_questionList"></activity>

        <activity
            android:name=".view.activity.common.QuestionScoreActivity"
            android:label="@string/title_activity_questionList"></activity>

        <activity
            android:name=".view.activity.common.ReadMeActivity"
            android:label="@string/title_activity_readme"></activity>

        <activity
            android:name=".view.activity.common.QuestionAnalysisActivity"
            android:label="@string/title_activity_questionAnalysis"></activity>

    </application>

</manifest>